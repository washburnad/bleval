<div>
  <h1>Events</h1>
  <ul>
    <% @events.each do |event| %>
      <li>
        <%= link_to event_details(event),
          event_path(event) %>
      </li>
    <% end %>
  </ul>
  <%= link_to 'Create Event', new_event_path %>
</div>